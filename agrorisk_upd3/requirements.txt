numpy>=1.24
pandas>=2.0
requests>=2.28

# torch нужен только для дообучения (finetune/save_checkpoint).
# Для расчёта риска он не обязателен: инференс умеет работать на numpy.
# torch>=2.0

# Веб-интерфейс (python web.py) работает на стандартной библиотеке —
# ни Flask, ни других пакетов для него ставить не нужно.
